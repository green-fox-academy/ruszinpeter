'use strict'

// -  Create a variable named `baseNum` and assign the value `123` to it

let baseSum: number = 123;

function doubling(dNum: number) {
   return dNum = dNum * 2;
}
console.log(doubling(baseSum));